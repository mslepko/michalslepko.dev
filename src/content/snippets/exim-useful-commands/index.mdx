---
title: 'EXIM - useful commands'
description: ""
published: 2017-02-07
redirect_from:
            - https://snippets.slepko.co.uk/exim-useful-commands/
categories: "bash, Bash shell, centos, exim, linux, Linux, shell"
hero: ../../../defaultHero.jpg
featuredImage:
---
Print a listing of the messages in the queue

```bash
exim -bp
```

Print a count of the messages in the queue

```bash
exim -bpc
```

Force another queue run

```bash
exim -qf
```

Force another queue run and attempt to flush the frozen message

```bash
exim -qff
```

Force delivery of one message

```bash
exim -M email-id
```

View the log for the message

```bash
exim -Mvl message-id
```

View the body of the message

```bash
exim -Mvb message-id
```

View the header of the message

```bash
exim -Mvh message-id
```

Remove message from the queue without sending any error message

```bash
exim -Mrm message-id
```

Fail message to bounce the message to the Sender

```bash
exim -Mg message-id
```

Freeze a message

```bash
exim -Mf message-id
```

Thaw a message:

```bash
exim -Mt message-id
```

How many Frozen mails on the queue

```bash
exim -bpr | grep frozen | wc -l
```

Deleteing all Frozen messages

```bash
exim -bpr | grep frozen | awk {‘print $3’} | xargs exim -Mrm
```
